# bscRND
bscRND (temp name, stands for basic Renderer) is a Vulkan game engine which by far has only one empty window lmao
## Installation
```bash
git clone https://github.com/griesson-h/bscRND.git; cd bscRND; mkdir build; cd build; cmake ..; make
```
gonna make a better readme when the project will able to at least render one tringle
